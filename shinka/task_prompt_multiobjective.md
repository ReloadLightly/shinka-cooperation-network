Can ShinkaEvolve discover interpretable alternative network-selection
specifications that improve forecasts of defense-cooperation agreements and
defense spending relative to Kinne and Kang's predictive Model 3?

You modify the countries' network objective, not the scientific evaluator.
The native continuous-time stochastic actor-oriented model has
f_i^net(x,z)=sum_k beta_k s_ik(x,z), stochastic tie choices and partner
confirmation. The trusted R adapter implements actual RSiena 1.3.10 statistics.
It retains Model 3's country sample, empirical controls, network density and
defense-spending objective STRUCTURE. All included network and spending
coefficients are jointly re-estimated statistically from training observations;
you cannot supply coefficients. This is structural program evolution over a
native effect grammar, not coefficient tuning or the old 15-subset catalog.

The original study distinguishes efficiency from free riding. Its empirical
evidence favors efficiency: dense local networks are associated with lower
spending, while its additional interaction intended to capture free riding does
not support that explanation. Model 3 is the reference; do not substitute Model
4. Our forecasting comparison does not independently establish either causal
mechanism. Lower predicted spending is not a reward and does not imply improved
security, a national strategy, optimal policy or a cost saving.

Return one Python program with build_network_spec(allowed_schema), whose only
statement returns a literal dictionary with exactly schema_version: 2 and
network_effects: [...]. Keep the EVOLVE-BLOCK markers. Each term is either an atom
{"effect": NAME, "parameter": INTEGER, "covariate": OPTIONAL_DECLARED_NAME}
or {"product": [TWO_OR_THREE_ATOMS]}. The supplied allowed catalog is
authoritative. Explain the hypothesized mechanism and structural change in code
comments. Candidate Python is AST-interpreted as literal data and never executed;
imports, calls, external-file access, arbitrary functions, evaluator edits and
outcome access are unsupported. No tools or external files are needed: use only
the authorized scientific context in the prompt, parent and inspirations.

The native catalog admits degree activity/popularity, raw/square-root degree
responses, truncated-degree knots, reciprocal-degree responses, degree
assortativity, several closure statistics, GWESP decay, Jaccard similarity,
four-cycles, distance-two and betweenness statistics, plus compatible products
with existing covariates. Multiple instances with different meaningful parameters
are allowed. There is no arbitrary effect-count limit or complexity reward;
source size is limited to 32 KiB, and native mathematical compatibility and
empirical identification still apply. Two-factor native interactions need an ego
factor or two dyadic factors. Three-factor interactions need two ego factors or
three ego/dyadic factors. The native grammar, rather than an invented utility
engine, determines which products are executable.

Do not jointly estimate proportional raw-degree moments from degPlus(1), inPop
and outAct, or proportional root-degree moments from degPlus(2), inPopSqrt and
outActSqrt. These are alternative actor-choice mechanisms, not identical models.
Likewise, GWESP(0) and transTies are different actor-choice mechanisms with the
same estimation statistic: select at most one as a main term, and do not estimate
both interactions with the same ego covariate factor(s). Native transTies counts
closure-status changes to other ego ties; GWESP(0) uses the proposed pair's
closure indicator. Genuine outTrunc knots are integers 1 through 159 for the
declared 161-actor universe. Knots >=160 reproduce the protected density
contribution and are excluded even as product factors.
egoX/altX/X are product modifiers only; the original main controls remain fixed
in structure. Aliases, factor ordering and comments do not create new models:
outTrunc2 canonicalizes to outTrunc, all degPlus parameters >=2 canonicalize to 2,
and Jout's ignored native parameter canonicalizes to 69. Native effect support
does not guarantee that a proposed combination can be estimated.
Repeated GWESP(0) factors are exactly idempotent: a product of two copies reduces
to one GWESP(0) atom, and repeated copies with a third modifier reduce to the
compatible two-factor product. This identity does not apply to transTies.
Canonicalization records confirmed identities; a different canonical hash is not
a guarantee of mathematical novelty under every possible statistic identity.

The following scientific objectives are fixed by the evaluator and all are
MAXIMIZED. For every t in {2006,2007,2008,2009}, fit only 1990 through t-1, then
simulate one unconditional annual interval from the observed t-1 network and
ordinal spending. Use only origin-available information, the declared rate and
covariate carry-forward, and the same seeds and 1,000-endpoint budget for every
compared specification. Define p_C(i,j,t)=sum_s A_C,s(i,j,t)/1000 and mean spending
zbar_C(i,t)=sum_s z_C,s(i,t)/1000. Average endpoints FIRST. Score common eligible
undirected pairs once, i<j, with pinned PRROC::pr.curve(...)$auc.integral.
Do not replace PRROC with average precision or average AUCs of hard networks.
Let B_t be original predictive Model 3 refitted on identical training data and
under the same estimation policy. Equal weights across the four years define:

J1(C) = mean_t [PR_AUC(C,t) - PR_AUC(B_t,t)].
J2(C) = mean_t [Brier(B_t,t) - Brier(C,t)].
J3(C) = mean_t [RMSE_spending(B_t,t) - RMSE_spending(C,t)].

Brier is the mean squared error of tie probabilities against binary outcomes;
it reflects more than calibration alone. Spending RMSE compares mean simulated
categories with observed categories, in the original ordinal units 1 through 11.
J3 rewards predictive accuracy, not lower spending. Every development year must
have an acceptable fit and valid predictions. Failed fits, timeouts, malformed
predictions or missing years have invalid fitness, never a partial-year average
or an invented poor scientific score. Retain the original strict convergence
thresholds, max|t|<0.1 and overall convergence<0.25.

Report and interpret the objective vector (J1,J2,J3), with reference (0,0,0).
Pareto dominance means at least as good in all objectives and strictly better in
at least one. Trade-offs remain visible. The declared auxiliary scalar
combined_score = 2 + (J1 + J2 + J3/10)/3
provides fixed preferences for native bandit/prompt adaptation; baseline is 2.
It does not replace the vector or determine Pareto dominance, and it is not
evidence that every objective improved. Do not change its weights or add runtime,
complexity, spending-level or qualitative bonuses. No LLM judges fitness.

Use parent selection, executable archive and top-performing inspirations,
measured development feedback, and available native recommendations to propose
meaningful changes. Identify each proposed mathematical structure independently
of code-embedding novelty. Explain expected gains and possible trade-offs rather
than asserting improvements before evaluation. Report stochastic uncertainty;
dependent dyads are not independent samples for a naive significance test.

Year 2010 remains outside every selection, mutation, novelty, inspiration, repair,
meta-recommendation and prompt-evolution context. It is reserved from this search,
though the historical paper already examined it. Final-year assessment is allowed
only after structure and protocol selection are fixed.
