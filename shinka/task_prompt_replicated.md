# Predictive network-specification discovery: replicated protocol

Propose an interpretable network-selection specification within the supplied
RSiena 1.3.10 native grammar. The reference is Kinne and Kang's Model 3:
degPlus(1) plus transTriads(0), with protected density, empirical controls and
spending-equation STRUCTURE. All included coefficients, including spending
coefficients, are jointly estimated from past observations. You choose native
statistics, internal functional parameters and compatible interactions, NOT
coefficient values, countries' actions, forecasts, outcome data or scoring rules.

Return one Python function build_network_spec(allowed_schema), whose sole
statement returns a literal dictionary with schema_version=2 and network_effects.
Preserve EVOLVE-BLOCK markers. Explain the mechanism, change from the parent and
possible trade-offs in comments. The trusted AST parser never executes candidate
Python. No imports, tools, files, helper calls, evaluator changes or new compiled
statistics are allowed. The supplied native catalog defines parameter meanings,
aliases, proportional-moment exclusions and interaction compatibility. A new
comment or alias is not a new mathematical specification. There is no arbitrary
term-count cap or complexity reward; native identification and the 32 KiB source
limit still apply.

For each development target 2006–2009, fit only 1990 through target-1. Use the
same approved precision-before-continuation policy for every specification:
strict individual ratios <0.1, overall <0.25 and native validity; eligible short
ratio failures receive one prespecified 3,000-draw fixed-vector reassessment.
A converged fit is an admissibility condition, not predictive success.

Every complete candidate has FIVE independent 1,000-endpoint forecast batches
per year. Average probabilities and spending over all 5,000 endpoints BEFORE
scoring. Maximize the equally weighted annual candidate-versus-reference vector:
J1 = PRROC integral PR-AUC(candidate) - PR-AUC(reference);
J2 = Brier(reference) - Brier(candidate);
J3 = ordinal-spending RMSE(reference) - RMSE(candidate).
All four years must be valid. Invalid fits never acquire invented losses;
interruptions and unavailable inputs are pauses, not evidence of a bad mechanism.
Canonical duplicates reuse their first committed numerical evidence and seeds.

The five delete-one-batch contrasts estimate conditional simulation uncertainty.
These approximate intervals omit fitting, historical generalization and causal
uncertainty; nonsmooth ranking and ties limit their accuracy. The archive retains
models not MC-resolved dominated. A resolved comparison must also be a point
Pareto comparison; an unresolved tournament is decided uniformly, so inconclusive
intermediate models can reproduce. This is an exploratory heuristic, not a
statistically certified superiority test.

Operational diversity and bandit/prompt credit use frozen REFERENCE-only scales:
1-mean(reference PR-AUC), mean(reference Brier), mean(reference spending RMSE).
They represent fractional reductions in reference deficits, not arbitrary raw-unit
sums. The operational scalar is 2+tanh(mean(J/scale)-t4*jackknifeSE(mean(J/scale))).
It is a fixed uncertainty-averse search preference, not another scientific
objective. Runtime, code length, low spending, geographic favoritism and persuasive
prose confer no reward. Do not alter these definitions in evolved prompts.

Measured starting evidence: GWESP(69), alpha=.69, had mixed development results:
The original Step 4 J=(-0.002670775,+0.000011270,-0.000217121) is preserved.
The new integer-count pooling view has J1 approximately -0.002706827; J2 and J3
are unchanged. Pooling now sums network successes before one division so equal
counts remain tied. This read-only numerical correction is not new evidence.
GWESP did not repair 2007 triangle excess:
observed751, reference approximately875.787, GWESP approximately891.998. This does
not establish that every GWESP specification fails or identify the misfit's cause.
Use parent/inspiration results, annual scores, structural diagnostics, numerical
warnings and native meta-memory to formulate the next hypothesis; do not assert
that a plausible mechanism has improved anything before evaluation.

The paper's selection–influence coevolution is retained. This predictive extension
does not independently establish efficiency versus free riding, improved security,
a national strategy or optimal partnerships. Lower predicted spending is not
fitness. Reserved target 2010 must never enter proposal, archive, novelty, meta or
prompt-evolution contexts. No final test or historical full-period inference is
part of this campaign's development loop.
